// tests/profile.spec.ts 
import { test, expect } from '@playwright/test'; test.describe('Profile verification', () => {  test('shows the correct profile data', async ({ page }) => { // 1) Go to profile page await page.goto('TODO: /profile or profile URL'); // 2) (Optional) Login steps if required by your app // await page.getByRole('button', { name: 'Sign in' }).click(); // await page.getByLabel('Email').fill('TODO'); // await page.getByLabel('Password').fill('TODO'); // await page.getByRole('button', { name: 'Continue' }).click(); // 3) Verify visible profile fields const fullName = 'TODO: expected full name'; const username = 'TODO: expected username'; const bio = 'TODO: expected bio'; // Replace selectors with yours: await expect(page.locator('TODO: selector for full name')).toHaveText(fullName); await expect(page.locator('TODO: selector for username')).toHaveText(username); await expect(page.locator('TODO: selector for bio')).toHaveText(bio); // 4) If you have an avatar image // const avatarUrl = 'TODO: expected avatar url (or partial string)'; // await expect(page.locator('TODO: selector for avatar image')).toHaveAttribute('src', avatarUrl);  }); });
